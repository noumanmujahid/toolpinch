document.addEventListener('DOMContentLoaded',()=>{const btn=document.querySelector('.menu-btn');const menu=document.querySelector('.mobile-nav');if(btn&&menu){btn.addEventListener('click',()=>menu.classList.toggle('open'))}const search=document.getElementById('toolSearch');const empty=document.querySelector('.empty-state');if(search){search.addEventListener('input',()=>{const q=search.value.toLowerCase().trim();let shown=0;document.querySelectorAll('.tool-card').forEach(card=>{const hit=card.innerText.toLowerCase().includes(q);card.style.display=hit?'block':'none';if(hit)shown++});if(empty)empty.style.display=shown?'none':'block'})}});
function copyText(id){const el=document.getElementById(id);if(!el)return;const text=el.value||el.innerText;if(!text)return;navigator.clipboard.writeText(text).then(()=>showToast('Copied to clipboard.')).catch(()=>showToast('Copy failed. Please select and copy manually.'))}
function clearFields(ids){ids.forEach(id=>{const el=document.getElementById(id);if(el)el.value=''});const result=document.getElementById('result');if(result)result.innerHTML='Result will appear here.'}
function showToast(message){let toast=document.querySelector('.toast');if(!toast){toast=document.createElement('div');toast.className='toast';toast.style.cssText='position:fixed;left:50%;bottom:22px;transform:translateX(-50%);background:#0f172a;color:#fff;padding:10px 14px;border-radius:999px;box-shadow:0 12px 35px rgba(15,23,42,.18);z-index:99;font-weight:700;font-size:14px';document.body.appendChild(toast)}toast.textContent=message;toast.hidden=false;clearTimeout(window.__toastTimer);window.__toastTimer=setTimeout(()=>toast.hidden=true,1800)}
